已完成只读分析，结论如下。

## 1. 应该改的具体位置（公开可见、像草稿/示例）

- `src/App.tsx:1884` `示例博客 / 2026-06-11` 与 `:1887` `阅读示例文章`：博客页头条区直白写“示例”，最显眼的草稿感来源。改为真实文章口吻，如标签去掉“示例”、按钮改“阅读全文”。注意此卡片 `onOpenPost(featuredPost)` 实际指向 `blogPosts[0]`（Legal RAG 复盘，真实内容），所以只是文案问题，不影响跳转。
- `src/App.tsx:823` `补运行截图和核心流程`、`:827` `补架构图、技术栈和数据流`：案例发现区 `case-route-strip` 三步条，前两步是“补…”的内部待办口吻（第三步 `:831` 已是正常陈述）。改为面向访问者的内容说明，如“运行截图与核心流程”“架构图、技术栈与数据流”，与第三步语气统一。

## 2. 属于真实路线图，保留或轻微改写

- `src/App.tsx:1261` 和 `:1335` 的 `nextSteps` 含 `补充真实运行截图`：位于项目详情页“后续扩展/扩展计划”板块（标题见 `:1455-1456`，渲染于 `:1459`），本质是合法路线图，保留。但“真实”二字暗示现有截图是假的，建议轻改为“补充运行截图”。
- 其余 `nextSteps`（如 `:993 :1019 :1045 :1071 :1097` 等，“接入真实 Embedding”“整理 ER 图”“接入 Web 试玩包”等）是正当扩展计划，保留不动。
- 各处“后续可…/后续接入…”叙述（`:856 :985 :1259` 等）是技术边界说明，保留。
- 案例状态标签 `整理中`（`:806`、portfolio `status`）是合法状态枚举，保留。

## 3. 不应改，避免扩大范围

- `src/data/portfolio.ts:183-188` 的 `articles`（含 `待补图`/`待整理`）：全仓库仅定义、无任何渲染（已确认 `articles` 未被 import/使用），属死代码，不可见，不在文案收口范围。如要清理应作为单独的代码清理任务。
- `src/data/portfolio.ts:34` `pending: '待整理'`：`statusLabels` 中的枚举，但无任何项目用 `status: 'pending'`，页面不会出现，跳过。
- 检索确认：`面试`、`作品集`、`占位`、`placeholder` 在这两个文件中**零命中**，无需处理。

## 4. 验证命令与浏览器 QA 路由

```bash
npm run lint && npm run build
```

浏览器 QA（建议 dev 环境逐一核对，浅色/暗色各一遍）：
- `/blog`：确认头条区不再出现“示例博客/阅读示例文章”，点击头条按钮仍正确打开 Legal RAG 复盘文章。
- 首页或案例发现区（`case-route-strip` 所在区块）：确认三步条文案统一、无“补…”待办感。
- 任一项目详情页（如 Legal RAG、Ozon ERP）：确认“后续扩展”板块文案正常，若改了“补充真实运行截图”需复核两处（通用 AI、通用业务模板各一）。
- 全站快速扫读，确认无残留“示例/补…”可见文字。

改动集中在 `src/App.tsx`，预计 4 处文案，零结构与逻辑变更。
